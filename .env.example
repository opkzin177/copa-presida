# Copie para variáveis de ambiente no Netlify (Site settings → Environment variables)

# PagBank API (opcional — sem isso o site usa os links estáticos pag.ae)
PAGBANK_TOKEN=seu_token_aqui
PAGBANK_ENV=sandbox
# ou production

# Admin da área restrita
ADMIN_USER=copa5
ADMIN_PASS=troque-esta-senha-forte

# Firebase já está hardcoded no front (RTDB público de leitura/escrita controlada pelo app)
# NUVEM=https://copa-presida-default-rtdb.firebaseio.com/presida_v2.json
